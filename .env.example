# Copy this to .env on your EC2 server and fill in the values
# cp .env.example .env && nano .env

# MySQL root password (used by all services)
MYSQL_ROOT_PASSWORD=KsrFruits@2026!

# JWT secret — generate with: openssl rand -hex 64
JWT_SECRET=paste_64_char_hex_here

# Gmail App Password for OTP emails
# Get from: myaccount.google.com → Security → App Passwords
MAIL_USERNAME=ksrfruitshelp@gmail.com
MAIL_PASSWORD=tfrrkjlufrcmufm

# OTP mode: false = real emails sent, true = OTP shown on screen (dev only)
OTP_MOCK=false
